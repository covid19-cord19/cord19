import React from 'react';
import Header from './Header';
import AppContent from './AppContent';

const Root = () => {
	return (
		<>
			<Header />
			<AppContent />
		</>
	);
};

export default Root;
